# Shim

This directory contains the Zig build for the native Busker shim.

The source for the shim is in [../src/shim/](../src/shim/)

Use this command when you need to refresh the `h2o-zig` dependency pin in `build.zig.zon`.

```bash
zig fetch git+https://github.com/outskirtslabs/h2o-zig.git
```
